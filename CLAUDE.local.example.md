# 本機設備資料（範本）

新設備 clone 這個倉庫後，第一步先執行：

```bash
cp ~/.claude/CLAUDE.local.example.md ~/.claude/CLAUDE.local.md
```

然後填寫下列各節。`CLAUDE.local.md` 已被 `.gitignore` 擋下不會進版控；
全域 `CLAUDE.md` 末尾以 import 語法載入它，所以填好即自動生效。

沒做這一步也不會壞：實測（Claude Code 2.1.228）import 指向不存在的檔案時不會報錯，
只是把該行字面文字留在 context 裡，等於沒有本機資料而已。

> 撰寫注意：檔案內不要出現行首或獨立的 `@路徑` 字樣，那會被當成 import 語法。
> 需要提到路徑時用反引號包起來。

---

## 設備識別

<!-- TODO: 主機名（hostname）、機型（筆電／桌機／伺服器）、這台機器主要拿來做什麼 -->

## 作業系統與 Shell

<!-- TODO: 發行版與版本、kernel、架構、是否 WSL、shell、桌面環境、locale、時區 -->

## 硬體

<!-- TODO: CPU 核數、RAM、GPU（有無獨顯／CUDA）、磁碟容量與掛載點 -->

## 工具鏈

<!-- TODO: 已安裝的語言執行環境與工具及版本，並註明記錄日期；一併列出「未安裝」的常見工具，
     讓 Claude 不會假設它存在。版本是快照，過期時請以指令重新確認。 -->

## 常用路徑

<!-- TODO: 專案根目錄、資料碟／外接碟掛載點、下載目錄等 -->

## 網路與本機服務

<!-- TODO: 網路介面與網段（註明是否 DHCP）、本機常駐服務與連接埠、
     容器／虛擬機、內網或 VPN 位址。註明探測日期 -->

## 工作習慣與偏好

<!-- TODO: 慣用套件管理器、編輯器、常用指令 -->

### 禁區

<!-- TODO: 哪些目錄不要動、哪些指令不要跑（系統升級、磁碟清理、
     git 寫入操作、跨專案改動等） -->
